# Cloudflare Pages Configuration
# Documentation: https://developers.cloudflare.com/pages/configuration/

# Build configuration
[build]
command = "npm run build"
publish = "dist"

# Environment variables (set via Dashboard instead)
# RESEND_API_KEY
# FROM_EMAIL  
# ADMIN_EMAIL
